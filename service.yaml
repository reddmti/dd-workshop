apiVersion: v1
kind: Service
metadata:
  name: dd-workshop-api
  namespace: default
spec:
  selector:
    app: dd-workshop-api
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: LoadBalancer
